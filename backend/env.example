# Configuration Django de base
SECRET_KEY=votre-secret-key-super-securisee-ici
DEBUG=False
DJANGO_SETTINGS_MODULE=backendAPI.settings

# Base de données (sera automatiquement configurée par Render)
# DATABASE_URL=postgresql://user:password@host:port/database

# Configuration email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=votre-email@gmail.com
EMAIL_HOST_PASSWORD=votre-mot-de-passe-app-gmail

# OpenAI API
OPENAI_API_KEY=votre-cle-openai-ici

# Google OAuth (pour l'authentification)
GOOGLE_OAUTH_CLIENT_ID=votre-client-id-google
GOOGLE_OAUTH_CLIENT_SECRET=votre-client-secret-google

# CORS (origines autorisées)
CORS_ALLOWED_ORIGINS=https://optitab.net,https://www.optitab.net

# Configuration optionnelle pour les fichiers statiques
STATIC_URL=/static/
MEDIA_URL=/media/

# Cache (optionnel)
# REDIS_URL=redis://127.0.0.1:6379/1
